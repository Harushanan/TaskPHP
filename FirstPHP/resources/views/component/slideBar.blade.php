<section class="hero" id="booking">
  <img id="slideshow" src="{{ asset('image/image.png') }}" alt="Pet Boarding Icon" />
</section>



<!-- <script>
    const images = [];
    let imgearry = @json($allimg);

    imgearry.forEach(function(item, index) {
        images.push(item.image_path);
        console.log(index + ": ", item.image_path);
    });

  let index = 0;
  const slideshow = document.getElementById("slideshow");

  setInterval(() => {
    index = (index + 1) % images.length;
    slideshow.src = images[index];
  }, 5000);
</script> -->
